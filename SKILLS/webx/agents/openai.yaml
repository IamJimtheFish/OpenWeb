interface:
  display_name: "OpenWeb"
  short_description: "Use local OpenWeb MCP tools"
  default_prompt: "Use OpenWeb tools (webx.search, webx.open, webx.actions.list/execute, webx.session.*, webx.crawl.*, webx.store.query). Prefer static compact opens first; escalate to Playwright only when needed; require explicit user approval for irreversible actions."
